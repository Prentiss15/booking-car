# 🚀 คู่มือการนำระบบจองรถต้นเดือนขึ้นใช้งานบนคลาวด์และอินเทอร์เน็ต (สำหรับผู้เริ่มต้น ไม่มีพื้นฐานก็ทำได้)

ระบบนี้ถูกออกแบบให้เป็น **Zero-Dependency (ไม่ต้องลงโปรแกรมเพิ่ม ไม่ต้องติดตั้งฐานข้อมูลแยก)** โดยใช้ **PHP + SQLite ในตัว** ทำให้การนำขึ้นเซิร์ฟเวอร์หรือคลาวด์ทำได้ง่ายที่สุดในบรรดาระบบเว็บทั้งหมด

เลือกลักษณะการติดตั้งที่ท่านสะดวกตาม 3 ทางเลือกด้านล่างนี้ได้เลยครับ:

---

## 🏆 ทางเลือกที่ 1: แนะนำที่สุดสำหรับคนไทยและหน่วยงาน (ง่ายที่สุด ใช้งานได้จริง)
> **เหมาะสำหรับ:** ผู้ที่ไม่ต้องการยุ่งยากกับคำสั่งโปรแกรมเมอร์ อยากมีชื่อเว็บของตัวเอง (เช่น `www.booking-dci.com`) และมีทีมงานภาษาไทยคอยช่วยเหลือ

### สิ่งที่ต้องมี:
- เช่า **Web Hosting (โฮสติ้ง)** ของไทย ราคาประมาณ 400 - 800 บาท/ปี (หรือเดือนละ 50-70 บาท) เช่น:
  - [HostAtom](https://www.hostatom.com/)
  - [Ruk-Com](https://ruk-com.in.th/)
  - [Bangmod Enterprise](https://bangmod.cloud/)
  - หรือโฮสติ้งใดๆ ที่มีระบบ **cPanel** หรือ **DirectAdmin**

### ขั้นตอนการนำขึ้นเว็บ (ทำเพียง 4 ขั้นตอน):
1. **บีบอัดไฟล์ในเครื่อง:**
   - เข้าไปที่โฟลเดอร์ `จองรถ` ในเครื่องคอมพิวเตอร์ของคุณ
   - คลุมดำไฟล์และโฟลเดอร์ทั้งหมด -> คลิกขวา -> เลือก **"Compress to ZIP file"** (บีบอัดเป็นไฟล์ `.zip`)
2. **เข้าสู่ระบบจัดการโฮสติ้ง (cPanel / DirectAdmin):**
   - ล็อกอินเข้า Control Panel ของโฮสติ้งที่เช่าไว้
   - คลิกที่เมนู **"File Manager"** (จัดการไฟล์)
   - ดับเบิลคลิกเข้าไปที่โฟลเดอร์ `public_html` (หรือโฟลเดอร์หลักของเว็บ)
3. **อัปโหลดและแตกไฟล์:**
   - กดปุ่ม **"Upload"** แล้วเลือกไฟล์ `.zip` ที่เตรียมไว้
   - เมื่ออัปโหลดเสร็จ ให้คลิกขวาที่ไฟล์ `.zip` บนโฮสติ้ง แล้วเลือก **"Extract"** (แตกไฟล์)
4. **เสร็จสิ้น พร้อมใช้งานทันที! 🎉**
   - เปิดบราวเซอร์แล้วพิมพ์ชื่อโดเมนของคุณ เช่น `https://yourdomain.com`
   - ระบบจะทำงานทันที ไม่ต้องสร้าง Database MySQL ให้ยุ่งยาก เพราะระบบมีฐานข้อมูล SQLite และไฟล์ `.htaccess` คอยรักษาความปลอดภัยให้โดยอัตโนมัติ

---

## ☁️ ทางเลือกที่ 2: ใช้ Cloud ฟรีสากล (Render.com) ผ่าน Dockerfile
> **เหมาะสำหรับ:** ต้องการทดลองใช้งานฟรีบน Cloud มาตรฐานสากล

ระบบนี้มีไฟล์ `Dockerfile` และ `.dockerignore` เตรียมไว้ให้เรียบร้อยแล้ว:

1. สมัครใช้งานที่ [https://render.com/](https://render.com/) (ฟรี)
2. สร้าง Git Repository บน GitHub (หรือ GitLab) แล้วอัปโหลดโค้ดทั้งหมดขึ้นไป
3. ที่หน้า Dashboard ของ Render:
   - กดปุ่ม **New +** -> เลือก **Web Service**
   - เชื่อมต่อกับ Repository ที่อัปโหลดไว้
   - ตรงส่วน **Environment** เลือกเป็น **Docker**
   - กด **Deploy**
4. Render จะทำการ Build อิมเมจและเปิดให้เข้าใช้งานผ่าน URL เช่น `https://car-booking-xxxx.onrender.com` ภายใน 3-5 นาที!

---

## 💻 ทางเลือกที่ 3: ใช้ Cloud VPS ส่วนตัว (Ubuntu + Docker Compose)
> **เหมาะสำหรับ:** มีเครื่อง VPS เช่น DigitalOcean, Linode, AWS Lightsail

ระบบนี้มีไฟล์ `docker-compose.yml` สำหรับรันคำสั่งเดียวจบ:

1. คัดลอกโฟลเดอร์โค้ดขึ้นไปบนเซิร์ฟเวอร์
2. รันคำสั่ง:
   ```bash
   docker compose up -d --build
   ```
3. เว็บไซต์จะเปิดทำงานที่พอร์ต 80 ทันที โดยมีพื้นที่จัดเก็บข้อมูล SQLite แบบถาวร (Persistent Volume) ไม่สูญหายเมื่อรีสตาร์ตเซิร์ฟเวอร์

---

## 🔒 ข้อมูลความปลอดภัยและบัญชีผู้ดูแลเริ่มต้น

เมื่อเปิดใช้งานบนเซิร์ฟเวอร์จริงแล้ว อย่าลืมเข้าสู่ระบบเพื่อเปลี่ยนรหัสผ่านเริ่มต้นนะครับ:

| สิทธิ์ | Username | Password เริ่มต้น | ลิงก์เข้าสู่ระบบ |
|---|---|---|---|
| **Super Admin** | `superadmin` | `superadmin123` | `/admin/login.php` |
| **Admin ทั่วไป** | `admin` | `admin123` | `/admin/login.php` |

> **คำแนะนำด้านความปลอดภัย:**
> เมื่อเข้าใช้งานครั้งแรก ให้ล็อกอินบัญชี `superadmin` แล้วเข้าไปที่ **"ดูรหัสผ่านผู้ดูแล / จัดการบัญชี"** เพื่อเปลี่ยนรหัสผ่านของทั้งสองบัญชีให้เป็นรหัสลับของหน่วยงานทันทีครับ
